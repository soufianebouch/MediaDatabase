@model MediaLibrary.Models.DeleteMedia.DeleteFilmViewModel

@{
    ViewData["Title"] = "FilmDelete";
}

<h1>FilmDelete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>DeleteFilmViewModel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Titel)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Titel)
        </dd>
    </dl>

    <form asp-action="FilmConfirmDelete" asp-route-id="@Model.Id" method="post">
        <input type="submit" value="Delete" class="btn btn-danger" />

    </form>
</div>

<br />
@if (User.IsInRole("Admin"))
{
    <div>
        <a asp-action="FilmsList">Back to List (Admin)</a>
    </div>
}

<div>
    <a asp-action="FilmsList" asp-controller="Gebruiker">Back to List</a>
</div>
@model MediaLibrary.Models.ReviewMedia.FilmReviewModel

@{
    ViewData["Title"] = "MuziekReview";
}

<h1>MuziekReview</h1>

<h4>FilmReviewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="MuziekReview">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Review" class="control-label"></label>
                <input asp-for="Review" class="form-control" />
                <span asp-validation-for="Review" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Score" class="control-label"></label>
                <input asp-for="Score" class="form-control" />
                <span asp-validation-for="Score" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@if (User.IsInRole("Admin"))
{
    <div>
        <a asp-action="MuziekList">Back to List (Admin)</a>
    </div>
}

<div>
    <a asp-action="MuziekList" asp-controller="Gebruiker">Back to List</a>
</div>

